# {模块名} - 四层设计

## 模块内部状态

```python
@dataclass
class {ModuleName}State:
    # 模块级单一状态源
```

## 四层基础设施

| 层面 | 设计内容 |
|-----|---------|
| **数据规矩** | {类型定义、约束、默认值} |
| **数据存储** | {存储方式、持久化策略} |
| **数据流转** | {状态机、验证、转换逻辑} |
| **接口层** | {对外暴露的接口契约} |

## 对外接口契约

```python
class {ModuleName}Service(Protocol):
    # 模块对外接口定义
```

## 状态流转图

```mermaid
stateDiagram-v2
    [*] --> {初始状态}
    {初始状态} --> {下一状态}: {触发条件}
```
